{
  "name": "create_custom_server",
  "version": "1.0.0",
  "description": "this is create custom server",
  "main": "Server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jignesh",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
